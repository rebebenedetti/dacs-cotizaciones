export enum AppEvent {
    Login = 'user:login',
    Logout = 'user:logout'
}

export enum AppStorageKey {
    AccessToken = 'access_token',
    FCMToken = 'fcm_token',
    CurrentUser = 'CurrentUser'
}